info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{host}}/{{baseRoute}}"
  body:
    type: json
    data: |-
      {
        "name": "Parc Astérix",
        "city": "Plailly",
        "country": "France"
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
