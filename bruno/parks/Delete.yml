info:
  name: Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{host}}/{{baseRoute}}/:roller_coaster_od"
  params:
    - name: roller_coaster_od
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "firstName": "RémiR",
        "lastName": "Rubis"
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
