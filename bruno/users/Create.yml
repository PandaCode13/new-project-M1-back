info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{host}}/users"
  body:
    type: json
    data: |-
      {
        "email": "remi.rubis+test@gmail.com",
        "firstName": "Rémi",
        "lastName": "Rubis"
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
