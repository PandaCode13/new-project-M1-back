info:
  name: Get single
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/users/:user_id"
  params:
    - name: user_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
