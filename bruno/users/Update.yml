info:
  name: Update
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{host}}/users/:user_id"
  params:
    - name: user_id
      value: 2edf0c62-6074-43f1-94d0-04900749adfd
      type: path
  body:
    type: json
    data: |-
      {
        "firstName": "RémiR",
        "lastName": "Rubis"
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
